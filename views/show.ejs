<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css"> <!-- here we have not included public as we have already given public for setting up express.static, only for ejs file-->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

   <div class="main w-full min-h-screen bg-zinc-900 text-white p-10 text-white">
    <a class="text-green-300 hover:text-green-500  inline-block mb-10" href="/">back to home</a>
    <h1 class="text-3xl tracking-tighter"><%= filename.replace(".txt","") %></h1>
    <p class="text-xl mt-2"><%= filedata %></p>

   </div>
</body>
</html>